services:
  proxy:
    build:
        context: .
        dockerfile: ./proxy/Dockerfile
    environment:
      FUNCTIONS: "animus"
    volumes:
      - .:/app
    restart: always
    ports:
      - "8080:8080"
  animus:
    build:
        context: .
        dockerfile: Dockerfile
    environment:
      FUNCTION_TARGET: Animus
    volumes:
      - .:/app
    restart: always
